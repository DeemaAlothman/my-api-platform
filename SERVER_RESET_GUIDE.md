# 🔄 دليل إعادة تهيئة السيرفر (Server Reset Guide)

هذا الدليل يشرح كيفية حذف قاعدة البيانات بالكامل وإعادة بنائها ببيانات نظيفة وموحدة (UUIDs فقط)، بالإضافة إلى إصلاح مشاكل الـ API.

## ⚠️ تحذير
هذه العمليات ستقوم بحذف **جميع البيانات** الموجودة في قاعدة البيانات. تأكد من أخذ نسخة احتياطية إذا لزم الأمر.

---

## 🛠️ الخطوة 1: إيقاف الخدمات وحذف البيانات

نفذ الأمر التالي لإيقاف جميع الحاويات وحذف وحدات التخزين (Volumes) المرتبطة بها، مما يضمن حذف قاعدة البيانات القديمة بالكامل:

```bash
docker compose down -v
```

---

## 📥 الخطوة 2: تحديث الكود وبناء الصور

تأكد من سحب آخر التحديثات (التي تتضمن إصلاحات `GET /leave-balances` و `DELETE /roles/:id`) وبناء الصور من جديد:

```bash
git pull origin main
docker compose up -d --build
```

---

## 🌱 الخطوة 3: إدخال البيانات الأساسية (Seeding)

يجب تشغيل أوامر الـ Seed لكل خدمة لإنشاء البيانات الأولية بالترتيب الصحيح.

### 1. خدمة المستخدمين (Users Service) - **الأهم**
هذا الأمر ينشئ:
- جميع الصلاحيات (Permissions) بنظام UUID الموحد.
- الأدوار الأساسية (Super Admin, HR Manager).
- المستخدم المدير (admin / password123).

```bash
docker compose exec users npm run prisma:seed
```

### 2. خدمة الحضور (Attendance Service)
هذا الأمر ينشئ جداول العمل الافتراضية.

```bash
docker compose exec attendance npm run prisma:seed
```

### 3. خدمة التقييم (Evaluation Service)
هذا الأمر ينشئ معايير التقييم ودورات التقييم لعام 2026.

```bash
docker compose exec evaluation npm run prisma:seed
```

---

## ✅ الخطوة 4: التحقق من الإصلاحات

بعد الانتهاء، يمكنك اختبار الـ Endpoints التي كانت لا تعمل:

### 1. عرض جميع أرصدة الإجازات
```http
GET {{gateway_url}}/leave-balances/
Authorization: Bearer {{token}}
```
المتوقع: `200 OK` مع قائمة فارغة `[]` (لأنه تم تصفير البيانات) أو مصفوفة الأرصدة إذا قمت بإضافتها.

### 2. حذف دور (Role)
```http
DELETE {{gateway_url}}/roles/{{role_id}}
Authorization: Bearer {{token}}
```
المتوقع: `200 OK` مع رسالة `Role deleted successfully`.

---

## 📝 ملاحظات إضافية

- **توحيد IDs**: بعد هذه العملية، جميع الـ IDs في النظام (سواء للمستخدمين، الصلاحيات، أو الأدوار) ستكون من نوع UUID الطويل (مثل `549bf69d...`) ولن تظهر الـ IDs القديمة (مثل `perm-013`).
- **المستخدم المدير**: يمكنك الدخول باستخدام:
  - **Username**: `admin`
  - **Password**: `password123`
