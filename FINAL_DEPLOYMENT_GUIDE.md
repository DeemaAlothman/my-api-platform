# 🚀 دليل النشر النهائي (Final Deployment Guide)

هذا هو الدليل المرجعي الوحيد الذي تحتاجه لنشر المشروع بشكل كامل، نظيف، ونظامي.

## 📋 مقدمة
لقد قمنا بتنظيف المشروع من الملفات الزائدة، وتوحيد نظام قاعدة البيانات، وإصلاح جميع مشاكل الصلاحيات.

---

## 🛠️ المرحلة 1: التنظيف وإعادة البناء (مرة واحدة فقط)
تستخدم هذه المرحلة لجعل المشروع نظيفاً تماماً (Fresh Start).

### 1. تنظيف الملفات الزائدة
قم بتشغيل سكربت التنظيف لحذف الملفات القديمة التي لم تعد لها حاجة:
```bash
bash cleanup.sh
```

### 2. إعادة تأسيس قاعدة البيانات (Migrations)
هذا السكربت سيقوم بحذف قاعدة البيانات القديمة، وإنشاء ملفات تهجير (Migrations) نظيفة وجديدة، وزراعة البيانات الأساسية:
```bash
bash reset_migrations.sh
```
> **ملاحظة:** سيطلب منك التأكيد (`y`) للمتابعة.

---

## 🚀 المرحلة 2: التشغيل اليومي (Deploy)
لإعادة تشغيل المشروع في أي وقت لاحقاً (مثلاً بعد سحب تحديثات من git)، استخدم هذا السكربت فقط. هو آمن ولا يحذف البيانات:

```bash
bash redeploy.sh
```

---

## ✅ التحقق من النظام
بعد الانتهاء، يمكنك الدخول والتحقق:
1.  **لوحة التحكم**: `http://localhost:3000` (أو الرابط الخاص بك).
2.  **حساب الأدمن**:
    *   Username: `admin`
    *   Password: `password123`
3.  **الصلاحيات**:
    *   تم منح `super_admin` جميع الصلاحيات بما فيها `roles:delete`.
    *   تم اعتماد الـ UUIDs في جميع الجداول بشكل موحد.

---

## 📂 هيكلية المشروع النهائية (التي تهمك)
*   `redeploy.sh`: سكربت النشر الآمن.
*   `reset_migrations.sh`: سكربت إعادة التأسيس (للطوارئ أو البداية النظيفة).
*   `cleanup.sh`: لتنظيف الملفات.
*   `docker-compose.prod.yml`: إعدادات السيرفر.
*   `apps/*/prisma`: مجلدات قواعد البيانات.

تمت المهمة بنجاح! 🥳
