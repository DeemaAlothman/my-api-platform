# 🚀 دليل النشر النهائي (Final Deployment Guide)

هذا الدليل يشرح كيفية جعل المشروع "نظامي" (Standard) ونشره بشكل نظيف، مع تصفير كامل للتاريخ القديم.

## 📋 المتطلبات
*   تأكد من تثبيت Docker و Docker Compose.
*   تأكد من أنك في المجلد الرئيسي للمشروع.

---

## 🛠️ الخطوة 1: تنظيف المشروع ودمج التهجيرات (Squash)
هذه الخطوة تقوم بما يلي:
1.  حذف قاعدة البيانات بالكامل.
2.  حذف جميع ملفات `migrations` القديمة والمتشعبة.
3.  إنشاء ملف `migration` واحد جديد ونظيف لكل خدمة (يحتوي على الهيكل النهائي).
4.  إدخال البيانات الأساسية (Seeding) بما فيها صلاحية `roles:delete` لمدير النظام.

**شغل السكربت التالي:**

```bash
bash reset_migrations.sh
```

> **ملاحظة:** سيطلب منك التأكيد (`y`)، لأنه سيحذف جميع البيانات.

---

## 🚀 الخطوة 2: التشغيل اليومي (أو إعادة النشر لاحقاً)
بعد تنفيذ الخطوة الأولى، أصبح مشروعك الآن مستقراً.
إذا أردت لاحقاً سحب تحديثات جديدة وتشغيلها (دون حذف قاعدة البيانات)، استخدم:

```bash
bash redeploy.sh
```

---

## ✅ التحقق من الصلاحيات والبيانات
1.  **تسجيل الدخول**:
    *   **User:** `admin`
    *   **Pass:** `password123`
2.  **حذف دور (Role)**:
    *   تمت إضافة الصلاحية `roles:delete` للأدمن، يمكنك الآن تجربة حذف دور عبر الـ API: `DELETE /roles/:id`.
3.  **فحص الجداول**:
    *   جميع الخدمات الآن تعمل بانسجام، وخدمة Auth تستعمل نفس جداول Users بشكل صحيح.

---

## 📂 ملفات يمكنك حذفها الآن (اختياري)
بعد نجاح العملية، الملفات التالية لم يعد لها لزمة (لأن سكربت `reset_migrations.sh` قام بالواجب):
*   `add-evaluation-permissions-production.sql`
*   `add-leave-permissions.sql`
*   أي ملفات `.sql` أخرى في المجلد الرئيسي.

مبروك! مشروعك الآن Clean و Standard. 🥳
